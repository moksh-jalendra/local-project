// casio-data.js - Config Data (Used if we need presets later)
// Currently, notes are generated algorithmically in casio.js to support full range.
const CASIO_CONFIG = {
    range: { start: "C2", end: "C7" },
    instruments: ['PIANO', 'ORGAN', 'SYNTH', 'FLUTE']
};